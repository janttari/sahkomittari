#!/bin/bash

sql=$(cat <<EOF
PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE IF NOT EXISTS "asiakkaat" (
        "id"    INTEGER,
        "numero"        TEXT,
        "nimi"  TEXT,
        "info"  TEXT,
        PRIMARY KEY("id")
);
CREATE TABLE IF NOT EXISTS "kulutus" (
        "id"    INTEGER,
        "numero"        INTEGER,
        "kwh"   TEXT,
        "pulssit"       INTEGER,
        PRIMARY KEY("id")
);
COMMIT;
EOF
)
sqlite3 testi.db "$sql"
